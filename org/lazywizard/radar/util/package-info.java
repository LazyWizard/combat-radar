/**
 * Provides classes to ease common rendering tasks. These will likely end up in LazyLib at some point, but I will leave stub classes so existing code can continue functioning.
 */
package org.lazywizard.radar.util;
